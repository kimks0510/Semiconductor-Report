# AGENTS.md
## 프로젝트: SK하이닉스 GSM 이직 준비용 일일 뉴스 브리핑 자동화

## 1. 목적
사용자는 삼성전자 MX사업부 해외영업(북미) 재직자이며, SK하이닉스 GSM(Global Sales & Marketing)
직무 이직을 준비 중. 이 프로젝트의 목표는 **매일 AI/메모리 반도체 산업 뉴스를 자동 수집·정리해서
브리핑 형태로 저장**하는 것. 최종적으로는 이직 자소서·면접에서 쓸 수 있는 시장 이해도를 쌓는 것이 목적.

## 2. 실행 방식 (중요 — 토큰 관리)
Codex는 출력 토큰 제한이 있으므로 아래 작업을 **한 번에 몰아서 하지 말고 단계별로 분리 실행**할 것.
가능하면 `codex exec`(비대화형 모드)로 각 단계를 별도 호출하고, 결과를 파일에 append하는 방식을 권장.

1단계: 뉴스 수집 (섹션 3 카테고리별로 각각 검색)
2단계: 수집한 원문을 섹션 5 포맷으로 요약/정리
3단계: 결과를 `/output/YYYY-MM-DD-briefing.md`로 저장

## 3. 수집 대상 카테고리 (매일 이 순서로, 카테고리당 최대 3~5개 기사)
1. SK하이닉스·삼성전자 메모리사업부 실적/컨퍼런스콜/조직 관련 뉴스
2. HBM/D램/낸드 현물가·고정가 동향 (트렌드포스 등 시장조사기관)
3. 엔비디아·AMD 등 GPU 업체 신제품/로드맵 발표
4. AWS·MS·구글·메타 등 하이퍼스케일러 AI 인프라 투자(캐펙스) 계획
5. ASML·TSMC 등 장비/파운드리 동향 (공급망 이해용)
6. SK하이닉스 채용공고 및 GSM 조직 관련 뉴스 (있는 경우만)

## 4. 우선순위 원칙
- 직전 성공 발송일 다음날부터 오늘까지의 뉴스를 빠짐없이 수집한다 (실행 스크립트가 직전 브리핑 날짜를 자동 계산해 프롬프트에 전달함). 직전 발송 기록이 없으면 최근 48시간을 기본값으로 한다.
- 공백이 5일을 넘으면 날짜별로 억지로 채우지 말고 누적 인사이트에 공백 기간을 간결하게 요약한다.
- 원문 출처(기사 링크) 반드시 포함 — 나중에 재검증 가능해야 함
- 확실하지 않은 정보(추정치, 루머)는 "추정" 표기
- 카테고리에 해당하는 뉴스가 없는 날은 "해당 없음"으로 표기하고 스킵 (억지로 채우지 말 것)

### 출처 신뢰도와 면접 활용 기준 (필수)
- **1순위 원문:** 기업 IR·실적자료·공식 뉴스룸·채용공고, 금융감독원·거래소·정부·규제기관, SEC 공시, 공식 컨퍼런스 발표와 제조사 기술문서. 숫자·일정·제품명은 가능하면 이 원문으로 확정한다.
- **2순위 산업 데이터:** TrendForce, Omdia, Gartner, Counterpoint, SEMI 등 시장조사·산업기관. 조사 기준일, 전망인지 확정치인지, 전분기 대비인지 전년 대비인지 함께 적는다.
- **3순위 주요 보도:** Reuters, Bloomberg, Financial Times, Wall Street Journal, Nikkei Asia, 연합뉴스 및 주요 경제지의 직접 취재 기사. 업계 반응·경쟁 구도·정책 의미를 보완하는 용도로 사용한다.
- 면접에서 인용할 핵심 주장과 숫자는 원칙적으로 1순위 출처 하나, 또는 서로 독립적인 신뢰도 높은 출처 두 곳 이상으로 교차검증한다.
- 단독 익명 보도는 `단독 보도/공식 확인 전`이라고 표시한다. 블로그, 커뮤니티, 홍보성 재가공 콘텐츠, 출처를 추적할 수 없는 SNS·유튜브는 근거로 사용하지 않는다.
- 기사마다 `출처 등급: 1차 원문 | 산업 데이터 | 주요 보도` 중 하나를 표시하고, 면접에서 말해도 되는 확정 사실과 아직 확인이 필요한 해석을 구분한다.

## 5. 출력 포맷 (각 기사당)
```
### [카테고리] 기사 제목
- 핵심 요약: (2~3문장, 숫자/팩트 위주)
- 왜 중요한가: (GSM 이직 준비 관점에서 한 줄)
- 출처: (매체명, 링크)
```

파일 최상단은 `3줄 총평` 다음에 `오늘 먼저 익힐 용어`를 배치한다. 기사 이해에 필수인 용어 4–7개를 아주 쉬운 뜻과 오늘의 연결고리로 먼저 설명한 뒤 누적 인사이트·지표·기사를 제시한다. 문서 마지막에는 `Wrap-up: 오늘의 용어와 더 익히면 좋은 용어`를 두어 본문 용어를 복습하고 다음 학습 용어를 제안한다. 용어를 기사 뒤에만 몰아두지 않는다.

### 누적 인사이트 (필수)
- 당일 보고서 작성 전에 `/output`의 기존 보고서를 읽고 문서 서두에 `## 누적 인사이트`를 작성한다.
- `최근 1주`에는 최근 7일 자료에서 반복되는 수요·가격·공급망 방향과 새롭게 바뀐 점을 3개 이내로 종합한다.
- `최근 1개월`에는 최근 30일 자료에서 구조적으로 이어지는 흐름, 상반된 신호, 향후 확인할 변수를 3개 이내로 종합한다.
- 단순 기사 제목 나열이 아니라 여러 날짜의 사실을 연결한 해석이어야 하며, 근거가 된 날짜 또는 지표를 괄호로 표시한다.
- 축적 자료가 부족하면 `데이터 축적 중`이라고 명시하고 존재하지 않는 추세를 만들지 않는다.

### 카카오톡 발송용 요약 (필수)
- 문서에 `## Kakao Summary` 섹션을 만들고 완결된 문장 3개를 번호 목록으로 작성한다.
- 각 문장은 제목을 포함해 카카오 API의 200자 제한을 넘지 않도록 본문 기준 최대 160자로 작성한다.
- 문장을 중간에서 자르거나 말줄임표로 끝내지 않는다.
- 1번은 메모리 가격·수급, 2번은 GPU·하이퍼스케일러·공급망, 3번은 GSM 영업 관점의 시사점으로 구성한다.
- 상세 수치와 기사 전문은 GitHub 전체 보고서 버튼으로 연결하므로 요약에 모든 내용을 억지로 넣지 않는다.

### 초심자 학습용 용어 설명 원칙 (필수)
- 영어 약어와 업계 용어는 처음 등장할 때 `약어(영문 원어, 쉬운 한국어 뜻)` 형식으로 설명한다.
  예: `CapEx(Capital Expenditure, 공장·장비·데이터센터 등에 쓰는 설비투자금)`.
- 약어를 풀어 쓰는 데 그치지 말고, 해당 지표가 메모리 수요·가격·GSM 영업에 어떤 의미인지 한 문장으로 설명한다.
- GPU, HBM처럼 자주 쓰는 용어도 당일 문서의 첫 등장에서는 반드시 설명한다. 독자가 전날 문서를 읽었다고 가정하지 않는다.
- 기사 본문의 흐름을 해치지 않도록 짧은 괄호 설명을 우선하고, 추가 설명이 필요한 용어는 문서 하단 `오늘의 용어` 섹션에 5~10개로 정리한다.
- `오늘의 용어`는 `용어 | 쉬운 뜻 | 오늘 뉴스에서의 의미` 3열 표로 작성한다.
- 전문용어로 다른 전문용어를 설명하지 말고, 반도체 초심자가 별도 검색 없이 이해할 수 있는 표현을 사용한다.
- `DRAM`, `NAND`, `QLC`, 제품 코드명, 장비명, 랙 구성명처럼 업계 종사자에게 익숙한 표현도 초심자에게는 생소하다고 가정한다.
- 각 기사에서 새 전문용어가 2개 이상 등장하면 출처 앞에 `- 초보자 주석:` 항목을 추가해 용어별 쉬운 뜻과 기사에서의 역할을 설명한다.
- 제품 코드명은 회사명과 제품 종류를 함께 설명한다. 예: `Kyber NVL144(엔비디아가 추진하는 144개 GPU 연결형 AI 서버 랙 설계)`.
- 문서 작성 후 대문자 약어, 영문 제품명, 공정·패키징 명칭을 다시 훑어 첫 등장 설명 또는 `오늘의 용어` 등재가 누락되지 않았는지 점검한다.
- 독자가 `그래서 이것이 메모리 수요에 어떤 영향을 주는가?`를 이해할 수 있도록 용어 정의 뒤에 오늘 기사와의 연결 의미를 덧붙인다.
- 숫자 범위에는 물결표(`~`)를 사용하지 않는다. GitHub가 취소선으로 오해할 수 있으므로 `13–18%`, `24–48시간`처럼 en dash(`–`)를 사용한다.

## 6. 배경 지식 참고 (분류·설명에 활용)
- 반도체 8대 공정: 웨이퍼제조→산화→포토(노광)→식각→박막증착→이온주입→금속배선→EDS/패키징
- 메모리 제품군: D램(휘발성), 낸드플래시(비휘발성), HBM(AI서버용 고대역폭메모리, TSV로 적층)
- 사업모델: IDM(삼성/SK하이닉스), 팹리스(엔비디아), 파운드리(TSMC)
- AI-메모리 연결고리: GPU(엔비디아 설계, TSMC 생산) 옆에 HBM이 붙어서 데이터 고속 공급 →
  하이퍼스케일러의 AI 데이터센터 투자가 메모리 수요의 핵심 동인

뉴스가 이 프레임 중 어디에 해당하는지 짧게 태깅해줄 것 (예: "[HBM/패키징]", "[하이퍼스케일러 캐펙스]").

## 7. 스케줄
- 매일 1회, 평일 기준 오전 8시 실행 목표 (cron: `0 8 * * 1-5`)
- 실행 실패 시 에러 로그를 `/output/errors.log`에 남길 것
- 저장 경로는 반드시 프로젝트의 `/output/YYYY-MM-DD-briefing.md`로 하고 파일명과 문서 제목 모두 실행일을 포함한다.
- 생성 완료 후 카카오톡 연동이 설정돼 있으면 `scripts/Send-KakaoBriefing.ps1`로 나와의 채팅에 요약을 발송한다.

## 8. 하지 말아야 할 것
- 카테고리당 5개 초과로 과도하게 채우지 말 것 (요약 품질 저하 방지)
- 출처 없는 추측성 서술 금지
- 저작권 있는 기사 원문을 통째로 복사하지 말 것 — 반드시 자기 언어로 요약
